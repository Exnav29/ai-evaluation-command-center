# Runtime Data

This directory may contain private evaluation evidence generated by the
Evaluation Command Center.

Runtime databases, raw runs, model responses, prompts, logs, credentials,
and private artifacts must not be committed to the public repository.

The public repository contains schemas, methodology, software, and synthetic
demonstration data only.

See `examples/demo-data/` for safe example datasets.
